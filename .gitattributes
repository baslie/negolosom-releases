# Текст — LF в репозитории и в рабочей копии. Файлы README.md и CHANGELOG.md
# генерируются скриптом с LF-концами; без этого правила Windows-клон получил бы
# CRLF и дал ложные диффы при каждой перегенерации.
* text=auto eol=lf

*.png binary
